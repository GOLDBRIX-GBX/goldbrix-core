# Contributing to GoldBrix

GoldBrix is developed in the open. Propose changes via pull request against a
staging branch; direct pushes to `main` are not accepted. Every change must keep
the system buildable and the consensus rules intact.
